version: "2"
sql:
  - engine: "postgresql"
    queries: "query/"
    schema: "migrations/"
    gen:
      go:
        package: "authdb"
        out: "gen/"
        emit_json_tags: true
        emit_pointers_for_null_types: true
